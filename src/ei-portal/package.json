{
  "name": "enterprise-intelligence-portal",
  "private": true,
  "scripts": {
    "build": "esbuild index.jsx --bundle --format=iife --global-name=__EI_REACT__ --minify --loader:.js=jsx --loader:.jsx=jsx --define:process.env.NODE_ENV='\"production\"' --outfile=react-bundle.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
